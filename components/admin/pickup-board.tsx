/**
 * 관리자 수송 요청 보드.
 *
 * 실물은 `components/pickup/pickup-board.tsx` 에 있다 — 임역원 화면과 **같은 보드**를
 * 쓴다. 예전엔 관리자 전용이었는데, 임역원도 "우리 지구 사람들이 어디로 모이나" 를
 * 봐야 해서(동규님 요청) 공용으로 옮겼다. 둘이 갈라지면 한쪽만 고쳐진다.
 *
 * 이 파일은 옛 import 경로를 살려두기 위한 재수출이다.
 */
export { PickupBoard, type BoardRow } from "@/components/pickup/pickup-board";
