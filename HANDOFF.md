# carbus-web 개선 작업 — 인수인계

> **다음 세션은 이 문서부터 읽으세요.** 마지막 갱신 2026-07-27.

---

## 0. 다음 세션 시작점 — 30초 요약

**Phase 3 배포 완료 (2026-07-27). 운영이 새 모델로 돌고 있습니다.**

- DB: `supabase db push` 로 `20260721050000~100000` 6개 적용. 자체검증 전건 통과.
  backfill 실측 — 상행 459 / 하행 459 / 미이용 60 (= 599).
- 코드: `phase3-trips` → `master` 머지 후 push. Vercel 배포 성공(커밋 `12797e8`).
- **§7-B 의 high 6건 전부 수정**. 같은 뿌리의 결함 3건이 더 나와 함께 고쳤습니다(§7-D).
- 검증: tsc · 122개 테스트 · eslint · build · 운영 백업 재현 기준선 일치.
  운영 데이터 실측 — 기존 배정 913건·리더 지정 92건 중 **새 규칙에 걸리는 것 0건**.
- 🟡 **아직 사람 눈으로 확인 안 된 것**: 관리자 화면(로그인 필요라 자동 검증 불가).
  `/admin/trips`·`/admin/buses`·`/admin/batch`·`/admin/leaders`·`/admin/changes` 를
  한 번씩 열어보세요. 특히 **하행 보기**가 편별 섹션으로 나뉘는지.
- 🔴 **문제가 생기면 롤백은 백업 복원뿐입니다.**
  `~/Backups/carbus-web/2026-07-27_2230-pre-phase3-deploy` (14개 테이블, 실패 0).

### 무엇이 달라졌나 (한 문장)

행사마다 **버스 편성을 화면에서 짤 수 있게** 됐고, **가는 편과 오는 편이 완전히 같은 구조**가
됐습니다. 그래서 다음 행사에서 "귀가 버스를 오후 3시·6시 두 편으로 나눈다" 같은 게
개발 없이 됩니다.

### 2026-07-27 세션에서 일어난 일

1. **§0-A 의 결정 3가지를 재확인.** ①·② 는 그대로, ③ 은 바뀌었다 —
   **여름수련회 정산이 끝나 곧 행사 전환이 필요하다**(Phase 3 배포 직후 후보).
2. **§7-B high 6건 전부 수정** (커밋 `7f0f7be`). 뿌리는 하나였다 — 3-C 가 신청에
   하행 편을 만들어놓고, 그 편을 읽어야 하는 자리들이 "하행 = 불린" 시절 코드로 남았다.
3. **같은 뿌리의 결함 3건을 추가로 발견·수정** (커밋 `48c332c`). §7-D 참고.
   그중 2건은 blocker `ab31181` 과 **정확히 같은 종류**다(화면 선택지 ⊋ 서버 허용 집합).
4. **배포 직전 백업이 운행편 테이블을 통째로 흘리고 있었다** (커밋 `11928f9`).
   백업을 실제로 떠보다 발견했다 — §7-E. 롤백 경로가 백업뿐인 배포에서 그 백업이
   불완전할 뻔했다.
5. 죽은 preset 모델(조합 셀) 제거 — 화면에서 다 빠졌는데 자기 테스트만 남아 있었다.

### 2026-07-21 세션에서 일어난 일

1. **§0-A 결정 3가지 완료.** 아래 §0-A 에 답이 적혀 있다.
2. **3-C 적대적 검증 완료.** 결함 후보 33건 → 병합 19건. **blocker 1건을 실제로 잡았다.**
3. **blocker 수정 + 커밋 `ab31181`.** 상행 배차 드롭다운이 통째로 비어 있어 관리자가
   화면에서 상행 수동 배차를 할 수 없던 문제. 자세한 내용은 §7-A.
4. **§2 의 롤백 주장이 거짓으로 판명.** 정정했다. 롤백 경로는 **백업 복원뿐**이다.
5. 행사 폴더화 설계 완료 → §8 에 Phase 4~6 재설계.

### 다음 세션에서 할 일 (순서대로)

1. **관리자 화면 눈으로 확인** (위 🟡). 배포는 끝났지만 로그인이 필요해 자동 검증을
   못 한 부분이다.
2. **행사 전환** — 정산이 끝났고 리더십 캠프 준비가 필요하다(§0-A ③).
   `/admin/control` 의 "새 행사 시작". 지난 행사 데이터는 삭제되지 않는다.
3. 3-D (납부 후 편성 변경 → 차액 목록 자동 표시, §0-A ① 참고).
4. 그 다음이 §8 의 Phase 4(행사 폴더화)다.

> 옛 컬럼(`departure_slot_id`·`uses_return_bus`)은 아직 살아 있고 파생 트리거가
> 동기화한다. 운영이 안정된 뒤 지운다 — **지우는 순간 구버전 앱 경로가 끊긴다.**

---

## 0-A. 사용자 결정 3가지 — ✅ 2026-07-21 확정

> 아래 각 항목의 **"→ 결정"** 줄이 답이다. 배경 설명은 결정 근거로 남겨둔다.

**① 환불 → 차액 목록에 자동으로 올린다.** 청구액은 건드리지 않고, 정산 화면
"차액 확인 필요" 목록에 자동으로 뜨게만 한다. 실제로 돌려줬는지는 사람이 화면에서 확인.
현장에서 이미 현금 정산한 경우를 시스템이 잘못 기록할 위험이 없어서 이 방식을 골랐다.
**미구현 — Phase 3 배포 후 3-D 로 별도 작업.** Phase 3 는 검증이 끝난 상태라
여기에 새 코드를 얹으면 검증을 다시 해야 해서 분리했다.

**② 배포 시점 → 지금 바로.** 다음 행사 신청이 안 열려 사용자 0명인 지금이 유일한
안전 구간이다. 단 §7 의 blocker 때문에 실제 배포는 그 수정 뒤로 밀렸다.

> **2026-07-27 재확인.** ① 그대로(배포 후 3-D). ② 그대로(사용자 0명, 오늘 배포).
> **③ 은 바뀌었다 — 여름수련회 정산이 끝났고 곧 행사 전환이 필요하다.**
> 아래 원문의 "정산 끝난 뒤 전환" 조건이 충족됐다는 뜻이다. Phase 3 배포 직후에
> `/admin/control` 에서 전환하면 된다(지난 행사 데이터는 삭제되지 않는다).

**③ 행사 전환 → 질문 자체가 재설계로 이어졌다.** 조사해보니 사용자 기억이 맞았다 —
과거 행사는 삭제되지 않고 `/admin/control` 에 "이 행사로 전환" 버튼이 실재하며
데이터는 몇 번을 오가도 닳지 않는다. 다만 **한 번에 한 행사만** 열리고 그게 전역이라,
master 가 과거를 열어보면 **모든 사용자 화면이 같이 과거로 간다.** 이 구조를 폴더처럼
바꾸는 것이 새 Phase 4 가 됐다. **§8 참고.** 그때까지의 실무 지침은 아래 ③ 원문과 같다
(여름수련회 정산이 끝난 뒤 전환하는 쪽이 안전).

---

## 0-B. (기록) 결정 당시의 배경 설명

### ① 납부한 사람의 편성을 바꾸면 환불은 어떻게 하나 ⚠️ 가장 중요

**무슨 일인가:** 어떤 학우가 왕복 5만원을 이미 냈는데, 나중에 "안 갈래요" 해서
편성을 비웠다고 하자. 시스템은 이 사람 청구액을 **5만원 그대로 둔다.** 그리고
정산 화면의 "차액 확인 필요" 목록에도 **안 나온다.** 즉 5만원을 돌려줘야 하는데
아무도 모른다.

**왜 그런가:** Phase 2-A 에서 "낸 순간의 금액을 얼려둔다"고 정했기 때문이다.
그때는 편성을 바꿀 일이 드물어서 문제가 안 됐는데, 이번에 가는 편·오는 편을
따로 끄고 켤 수 있게 되면서 훨씬 자주 생기게 됐다.

**지금 해둔 것:** 편성을 바꿀 때 화면에 경고만 띄운다.

**사용자가 정할 것:** 자동으로 환불 기록을 남길지, 아니면 지금처럼 사람이 보고
판단할지. 이건 §5 의 "차액 46명 / 135만원" 과 같은 성격의 문제다 — 현장에서
현금으로 이미 정산했는지는 사용자만 알기 때문이다.

### ② 운영에 언제 올릴까

**무슨 일인가:** 이번 변경은 DB 와 코드를 **동시에** 올려야 한다. 한쪽만 올리면
관리자 화면이 전부 깨진다(컬럼 이름이 바뀌어서).

**지금 왜 괜찮나:** 여름수련회가 6/27 에 끝났고 다음 행사 신청이 아직 안 열려서
쓰는 사람이 없다. 그래서 잠깐 창이 생겨도 아무도 안 겪는다.

**사용자가 정할 것:** 지금 올릴지, 리더십 캠프 준비를 시작하기 직전에 올릴지.
**다음 행사 신청이 열린 뒤에는 이 방법을 쓰면 안 된다.**

### ③ 리더십 캠프 편성을 언제 짤까

**무슨 일인가:** 편성 화면은 **활성 행사만** 만질 수 있다. 즉 리더십 캠프 편성을
짜려면 먼저 "새 행사 시작"을 눌러 활성 행사를 바꿔야 하고, 그러면 여름수련회
화면이 비워진다(데이터는 남고 되돌릴 수 있다).

**사용자가 정할 것:** 여름수련회 정산·환불이 다 끝난 뒤에 전환할지, 아니면
전환해두고 정산은 지난 행사로 되돌려가며 할지.

---

## 1. 지금 어디까지 왔나

여름수련회(6/23~27) 운영 후 받은 피드백 9건을 6단계로 나눠 고치는 중입니다.
**0~2단계는 끝났고 운영에 반영돼 있습니다.**

| 단계 | 내용 | 상태 | PR |
|---|---|---|---|
| 0 | 거짓말 제거 (관측 교정) | ✅ 배포됨 | [#8](https://github.com/Lumiere001/carbus-web/pull/8) |
| 1 | 행사(event) 골격 + 비파괴 초기화 | ✅ 배포됨 | [#9](https://github.com/Lumiere001/carbus-web/pull/9) |
| — | 정산 경로 행사범위 복구 (Phase 1 회귀 핫픽스) | ✅ 배포됨 | [#10](https://github.com/Lumiere001/carbus-web/pull/10) |
| 2-A | 결제 장부 + 환불 차액 추적 | ✅ 배포됨 | [#11](https://github.com/Lumiere001/carbus-web/pull/11) |
| — | 행사별 차량비 설정 + 용어 '원장'→'장부' | ✅ 배포됨 | [#12](https://github.com/Lumiere001/carbus-web/pull/12) |
| 2-B | 취소 상태 + 좌석 자동 반납 | ✅ 배포됨 | [#13](https://github.com/Lumiere001/carbus-web/pull/13) |
| 3-A | 배차 특례 플래그화 + 운행편 모델(event_trips) | 🟡 로컬만 (미배포) | 브랜치 `phase3-trips` |
| 3-B | 편성 편집 UI (/admin/trips) | 🟡 로컬만 (미배포) | 〃 |
| 3-C | 신청 대칭화 (up/down_trip_id + 폼 + 엔진) | 🟡 로컬만 (미배포) | 〃 |
| — | 배차 드롭다운 blocker 수정 (§7-A) | 🟡 로컬만 (미배포) | 커밋 `ab31181` |
| 3-D | 납부 후 편성 변경 → 차액 목록 자동 표시 | 대기 (§0-A ①) | — |
| **4** | **행사 폴더화 — 행사별 독립 운영 (§8)** | 대기 · 신설 | — |
| 5 | 비고 구조화 본체 (transport_legs) | 대기 (구 Phase 4) | — |
| 6 | 화면 응집 (목업 반영) | 대기 (구 Phase 5) | — |

> **순서를 바꾼 이유 (2026-07-21).** 원래 4=비고 구조화, 5=화면 응집이었다.
> 행사 폴더화를 그 뒤로 미루면 같은 일을 두 번 한다. 화면 응집은 모든 화면에
> "지금 어느 행사를 보고 있나"를 붙여야 하는데 그 구조가 지금 바뀌는 중이고,
> 비고 구조화는 새 테이블(`transport_legs`)을 만드는 작업인데 §4 규칙 8 때문에
> 그 테이블의 행사 범위 설계가 폴더화 결정에 종속된다. 먼저 정하고 나서 만든다.
> **v2.0.0 태그는 Phase 4·5·6 이 전부 끝난 뒤에 붙인다** (사용자 결정).

**피드백 ↔ 단계 매핑**

| 사용자 피드백 | 해소 단계 |
|---|---|
| 상/하행 버스·출발시각 자유 선택 | **Phase 3** |
| 초기화 버튼 | Phase 1 ✅ |
| 전체 순장/순원 편집 UX (최상단 폼) | Phase 6 |
| 비고 구조화 (타지구·KTX 확정상태) | Phase 5 |
| 출석 호차별 네비게이션 | Phase 6 |
| 부분참 정보 산발 | Phase 6 |
| 로그 50건 제한 | Phase 6 |
| 변경 이력 추적 | Phase 6 |
| 캠퍼스 송금 등록 안 함 | Phase 2-A 일부 ✅ (넛지 UI는 Phase 6) |

> 번호는 2026-07-21 재배치 기준이다(구 4→5, 구 5→6). 새 Phase 4 는 행사 폴더화(§8).

---

## 1-A. ⚠️ 먼저 알아야 할 것 (2026-07-21 추가)

**`.env.local` 은 운영 DB 를 가리킨다** (`qqtqwyhclscfjlefkiqr.supabase.co`).
`pnpm dev` 를 그냥 띄우면 **로컬 화면이 운영 데이터에 붙는다.** 로컬 스키마로 검증하려면
`.env.local` 을 `supabase status` 의 로컬 URL·키로 바꾸고 띄워야 한다.
지금 브랜치의 코드는 `buses.up_trip_id` 를 조회하는데 운영은 아직 `departure_slot_id` 라,
바꾸지 않고 띄우면 /admin 화면이 전부 깨진다.

**브랜치 `phase3-trips` 는 아직 운영에 반영되지 않았다.** 커밋 14개가 로컬 검증만 끝난 상태다.
반영 순서는 §3 "운영 반영" 참고 — 이 브랜치는 **DB 와 코드를 함께** 올려야 한다
(컬럼 rename 이라 한쪽만 올리면 깨진다). 다행히 행사 사이 정지 구간이라 트래픽이 없다.

## 2. Phase 3 — 무엇을 어떻게 만들었나

**목표(사용자 확정):** "행사에 관계없이 항상 쓸 수 있는 **범용 틀**". 하행은 상행과 완전 대칭 —
"용어만 하행으로 바뀔 뿐". 여름수련회 기존 데이터는 그대로 두고 재배차하지 않는다.

### 3-A ✅ 끝남 (로컬 검증 완료, 미배포 — 브랜치 `phase3-trips`)

1. **배차 특례 플래그화** — `buses.is_cohesion_exempt` / `fill_priority` / `display_order`.
   엔진이 `"1호차"` 문자열 대신 이 컬럼을 본다. 배차 결과는 비트 단위로 동일.
2. **`departure_slots` → `event_trips`** (rename) + `direction`(up/down)·`departs_at`·
   `origin`·`destination`. `buses.departure_slot_id` → `up_trip_id`(nullable) + `down_trip_id`.
   행사마다 하행 편 1건 생성 + 전 차량 연결.
3. **골든 스냅샷** `tests/unit/batch-golden.test.ts` — 운영 599명 형상을 익명화해 고정.
   기존 33개 테스트로는 특례를 통째로 지워도 31개가 통과했다(하행 응집 면제는 테스트 0개).

### 3-B ✅ 편성 편집 UI

`/admin/trips` — 운행편·차량을 화면에서 만들고 고친다. 상·하행이 같은 컴포넌트라
하행을 여러 편으로 나눠도 코드 변경 없이 섹션이 나뉜다.
파괴적 조작은 **DB 트리거**가 막는다(화면 검사는 우회 가능해서). 차량 삭제·운행편 삭제·
방향 교차 지정·신청 편과 어긋나는 편 변경·마지막 활성 편 비활성화.

### 3-C ✅ 신청 대칭화

`registrations.up_trip_id / down_trip_id`. `attendance_type` 은 **파생값**이 됐다
(실측: 기존 599건과 100% 일치). 신청 폼은 조합 셀 대신 상행·하행 두 개의 독립 선택.
엔진도 하행을 상행과 같은 편별 그룹 배차로 바꿨다 — 골든 스냅샷 기대값 불변.

**양방향 파생 트리거**(`trg_reg_000_derive`)가 `registrations` 의 옛 컬럼과 새 컬럼을 서로 채운다.

> ⚠️ **정정 (2026-07-21).** 이전 판에 여기 "덕분에 DB 를 먼저 올려도 구버전 앱이 돌고,
> 배포 뒤에도 앱만 되돌리는 롤백이 가능하다" 고 적혀 있었다. **거짓이다. 그 문장을 믿고
> 배포하면 안 된다.**
>
> 파생 트리거가 덮는 것은 `registrations` 뿐이다. 3-A 가 rename 한 것은
> **`buses.departure_slot_id` → `up_trip_id`** 이고, `buses` 에는 하위호환 장치가 없다.
> `master` 브랜치 코드가 `buses.departure_slot_id` 를 읽는 곳이 실측 4곳 이상이다
> (`attendance/page.tsx:97`, `batch/actions.ts:59`, `batch/page.tsx:61`, `buses/page.tsx:29`).
> 그 컬럼은 마이그레이션 후 존재하지 않으므로 **앱만 master 로 되돌리면 출석·배차·차량
> 화면이 전부 깨진다.**
>
> **따라서 이 브랜치의 롤백 경로는 백업 복원뿐이다.** 배포 전 백업을 반드시 확보하고,
> 되돌릴 상황을 백업 복원 기준으로 계획할 것. (`v_bus_occupancy` 뷰는 옛·새 이름을 둘 다
> 노출해서 대시보드만 예외적으로 무사하다 — 이 예외를 일반화하지 말 것.)

⚠️ 이 트리거는 **ENABLE ALWAYS** 여야 한다. 백업 적재가 `session_replication_role=replica`
로 트리거를 끄는데, 이건 업무 로직이 아니라 파생 컬럼을 채우는 구조 유지 장치라
꺼지면 CHECK 위반으로 적재가 통째로 실패한다. 그리고 **`enable trigger user` 를 쓰는
블록이 지나갈 때마다 조용히 ORIGIN 으로 내려간다** — post-load.sh 가 매번 검사한다.

### 남은 것 (3-C 이후)

- **옛 컬럼 제거** — `departure_slot_id` / `uses_return_bus` 는 아직 살아 있다(파생 트리거가
  동기화). 운영이 안정된 뒤 별도로 지운다. 지우는 순간 앱 롤백 경로가 사라지므로 서두르지 말 것.
- **납부 후 편성 변경 → 환불** ⚠️ 사용자 결정 필요.
  실측: 왕복 5만원을 낸 사람의 편을 다 비워도 `fee` 는 50000 으로 **동결**되고
  장부 차액에도 안 잡힌다(Phase 2-A 가 청구액을 납부 시점에 동결하는 설계).
  3-C 로 편을 개별로 끄고 켜기 쉬워졌으니 이 상황이 훨씬 자주 생긴다.
  지금은 **편집 화면에서 경고만** 띄운다. 자동 환불 기록을 남길지는
  §5 의 "차액 46명" 항목과 함께 결정할 문제다.
- **편별 요금** — 요금은 아직 행사 단위(`events.fee_roundtrip` / `fee_oneway`)다.
  상행과 하행 구간이 다른 행사(예: 광주→무주 / 무주→서울)에서 방향별 요금이 필요해지면
  `event_trips.fare` 로 옮기는 별도 작업이 된다.

---

## 3. 재개 방법

### 로컬 환경 띄우기

```bash
open -a Docker && sleep 30          # Docker Desktop (필수)
cd ~/Projects/carbus-web
supabase start                       # 로컬 스택
supabase db reset --no-seed          # 마이그레이션 전량 적용
python3 scripts/local-verify/load-backup.py    # 운영 백업 적재 (최신 백업 자동 선택)
bash scripts/local-verify/post-load.sh         # 데이터 의존 backfill 일괄 + 기준선 출력
```

적재 후 아래와 일치해야 운영과 동일한 상태입니다 (post-load.sh 가 직접 찍어줍니다):

```
신청 599 · 감사로그 18,967 · 소속(home_unit_id) 65 · 장부 1,081 · 차액 46명
배차 상행 454 / 하행 459
```

> `load-backup.py` 는 `~/Backups/carbus-web/` 에서 **가장 최근 백업을 자동 선택**합니다
> (인자로 경로를 주면 그걸 씁니다). 상수로 박아두면 Phase 마다 낡습니다.
> ⚠️ 로컬 DB에 실명이 올라갑니다. 커밋·외부 전송 금지.

> ⚠️ **2026-07-21_0843-pre-phase2b 이전 백업은 못 씁니다.** `events`·`org_units`·
> `payment_ledger` 가 백업 대상에서 빠져 있어서, 적재하면 `registrations.event_id` 가
> 존재하지 않는 행사를 가리킵니다. 적재는 replica 모드(FK 끔)라 성공한 척하지만
> RLS·뷰를 통과하면 599행이 전부 0으로 보입니다. 지금은 `load-backup.py` 가
> 착수 전에 이 누락을 검사해 크게 실패시킵니다. 쓸 수 있는 최초의 완전한 백업은
> `2026-07-21_0924-pre-phase3` 입니다.

### 스키마 변경 작업 절차 (반드시 지킬 것)

```bash
# 1) 새 마이그레이션을 잠시 빼고 "적용 전" 기준선을 찍는다
mkdir -p /tmp/hold && mv supabase/migrations/<새파일>.sql /tmp/hold/
supabase db reset --no-seed && python3 scripts/local-verify/load-backup.py
bash scripts/local-verify/post-load.sh
bash scripts/local-verify/snapshot.sh > /tmp/before.txt

# 2) 되돌려놓고 적용
mv /tmp/hold/*.sql supabase/migrations/
docker exec -i supabase_db_carbus-web psql -U postgres -d postgres -v ON_ERROR_STOP=1 -q < supabase/migrations/<새파일>.sql

# 3) 대조 — 의도한 변경만 나와야 한다
bash scripts/local-verify/snapshot.sh > /tmp/after.txt
diff /tmp/before.txt /tmp/after.txt

# 4) 기능 테스트
bash scripts/local-verify/test-event-switch.sh   # 행사 전환
bash scripts/local-verify/test-ledger.sh         # 장부·청구액 동결
bash scripts/local-verify/test-cancel.sh         # 취소·좌석 반납

# 5) 코드 검증
pnpm tsc --noEmit && pnpm vitest run && pnpm eslint app components lib && pnpm build
```

### 운영 반영

```bash
# ⚠️ set -o pipefail 이 **필수**다. 백업 출력을 | tail 로 줄이면 파이프의 종료 코드는
#    tail 것이라, exit 1 안전장치가 통째로 삼켜지고 db push 가 그대로 진행된다.
#    2026-07-28 에 실제로 그렇게 넘어갔다.
set -o pipefail
node scripts/local-verify/backup-prod.mjs ~/Backups/carbus-web/$(date +%Y-%m-%d_%H%M)-pre-phaseN \
  && supabase db push          # link 되어 있음 (project ref qqtqwyhclscfjlefkiqr)
```

> `supabase db push`는 **DB 비밀번호가 필요한 link가 이미 되어 있어서** 그냥 됩니다.
> link가 풀리면 `supabase link --project-ref qqtqwyhclscfjlefkiqr`를 **사용자가 직접** 실행해야
> 합니다(비밀번호 입력 필요).
>
> ⚠️ CI는 마이그레이션을 적용하지 않습니다. **DB 먼저 적용 → 확인 → PR 머지** 순서를 지키세요.
> 코드만 먼저 배포되면 없는 테이블을 조회해 화면이 깨집니다.
>
> ⚠️ **브랜치 `phase3-trips` 는 예외입니다.** 컬럼 rename(`buses.departure_slot_id`
> → `up_trip_id`)이 들어 있어 **어느 쪽을 먼저 올려도 반대쪽이 깨집니다.**
> DB 적용과 코드 배포를 연달아 해야 합니다. 지금은 행사 사이 정지 구간이라
> (여름수련회 6/27 종료, `batch_enabled=false`) 실사용 트래픽이 없어 안전합니다.
> 다음 행사 신청이 열린 뒤에는 이 방법을 쓰면 안 됩니다.
>
> 🔴 **롤백은 백업 복원뿐입니다.** "앱만 되돌리면 된다"는 §2 의 옛 서술은 거짓으로
> 판명됐습니다(같은 절의 정정 박스 참고). 배포 직전 백업이 **성공했는지 파일 크기까지
> 확인한 뒤에** `db push` 하세요. 백업 없이 올리면 되돌릴 방법이 없습니다.

---

## 4. 반드시 지켜야 할 규칙 (실제로 사고를 막은 것들)

1. **backfill 중에는 사용자 트리거를 끈다.**
   `registrations`를 UPDATE 하면 감사 트리거가 599건의 "아무도 안 바꾼" 이력을 쌓는다.
   실제로 Phase 1에서 18,967 → 19,566으로 늘었다가 잡았다.

2. **뷰를 재작성할 때 원본 정의를 먼저 덤프해서 조건을 확인한다.**
   `v_payment_summary`에 `self` 제외 조건이 있었는데 빠뜨려 납부 집계가 60건 부풀었다.

3. **UNIQUE는 제약(`pg_constraint`)뿐 아니라 인덱스(`pg_indexes`)도 확인한다.**
   `buses.name` UNIQUE를 놓쳐 차량 복제가 통째로 막혔다.
   `snapshot.sh`에 인덱스 검사를 넣어뒀다.

4. **비고 텍스트로 자동 판정하지 않는다.**
   - `"환불해야 할 것은 없음"`이 '환불' 부분문자열에 걸린다(상계 건)
   - `"취소함 → 취소 이후 순수 하행 확정"`은 실제로는 참석자다
   - 반대로 아무 말 없이 차액이 생긴 28명은 놓친다
   **금액은 금액으로, 상태는 상태 필드로.**

5. **BEFORE 트리거는 이름 알파벳순으로 실행된다.**
   `registrations` 의 BEFORE 트리거는 4개가 아니라 **9개**다 (실측):
   `trg_cleanup_bus_fixed_on_reg_delete` → `trg_guard_attendance` → `trg_reg_00_fare`
   → `trg_reg_01_cancel` → `trg_reg_audit` → `trg_reg_block_delete`
   → `trg_reg_guard_assignment` → `trg_reg_guard_roles` → `trg_reg_updated_at`
   감사에 남겨야 할 변경은 `trg_reg_audit`보다 **먼저** 돌아야 한다.
   → 새 트리거는 `trg_reg_02_*` 처럼 지어야 앞에 선다. `trg_trip_*` 로 지으면
   `trg_reg_audit` 뒤로 밀려 **변경이 조용히 감사 누락**된다.
   (`snapshot.sh` 의 "### 트리거" 섹션이 현재 순서를 항상 찍어준다)

6. ~~**GENERATED 컬럼은 BEFORE 트리거에서 `NEW`가 항상 NULL이다.**~~ — **더는 사실이 아니다.**
   Phase 2-A 가 `fee` 의 생성식을 해제해서 지금은 평범한 nullable int 다
   (`is_generated=NEVER`, public 스키마에 생성컬럼 0개). 지금은 `trg_reg_00_fare`
   (BEFORE, 알파벳상 `trg_reg_audit` 앞)가 `NEW.fee` 를 채우므로 **Phase 2-A 이후
   새로 쌓이는 감사행은 `after_value.fee` 에 실값이 들어간다.**
   ⚠️ "after_value.fee 는 NULL" 을 전제로 쿼리를 짜면 옛 행과 새 행이 뒤섞여 틀린다.
   과거 46명을 복원할 때 쓴 성질이라 기록으로만 남긴다.

7. **`v_payment_summary` 를 CASCADE 로 날리지 마라.**
   원래는 "생성컬럼 해제는 `DROP EXPRESSION` 으로" 라는 규칙이었다(§6 참고).
   일반화하면: `registrations` 의 컬럼을 `DROP COLUMN` 하면 CASCADE 로
   `v_payment_summary` 가 함께 삭제돼 납부 화면이 즉시 죽는다.

8. **신규 테이블은 `event_id` + RESTRICTIVE 정책을 갖고 태어나야 한다.**
   안 걸면 다음 행사에서 지난 행사 데이터가 샌다.

9. **컬럼을 rename 하면 `as` 캐스트와 선택 필드가 그 변경을 숨긴다.** (2026-07-21, §7-A)
   `buses.departure_slot_id` → `up_trip_id` rename 후 `bus-options.ts` 만 옛 이름으로
   남았는데, 타입이 `departure_slot_id?: number` 로 **선택 필드**라 `tsc` 가 못 잡았고
   화면은 `(busRes.data ?? []) as BusInfo[]` 로 **캐스트**해 넘겨 그 검사마저 무력화돼 있었다.
   결과는 상행 배차 드롭다운이 통째로 빈 blocker 였는데 `tsc`·테스트 129개·빌드가 전부
   통과했다. 단위 테스트는 픽스처가 옛 키를 갖고 있어 원리적으로 못 잡는다.
   → **DB 컬럼을 담는 타입의 필드는 선택(`?`)으로 만들지 말고, Supabase 쿼리 결과에
   `as` 를 붙이지 마라.** 캐스트를 떼면 select 문에서 컬럼이 빠졌을 때 `tsc` 가
   `TS2322 — Property 'x' is missing` 로 잡는다(실측 확인).
   → rename 마이그레이션의 체크리스트에 **`grep -rn "<옛컬럼명>" app components lib` 로
   전수 확인**을 넣을 것. 옛 이름이 남아도 컴파일은 통과할 수 있다.

---

## 5. 열린 항목

### 🔴 사용자 확인 필요

- **차액 46명 / 1,350,000원** — `/admin/payments`의 "차액 확인 필요" 섹션.
  낸 돈이 현재 청구액보다 많은 사람들. **확정 채무가 아니라 계산상 차액**이다
  (현장에서 이미 현금 정산했을 수 있음). 17명은 비고에 환불 언급이 있고,
  **28명은 아무 언급이 없어 아무도 모르고 있었다.**
  → 정산 완료 / 아직 안 드림을 구분해 주시면 환불 기록 남기는 기능을 붙일 수 있다.

- **취소 처리가 안 된 기존 데이터** — 비고에 "취소"라 적힌 사람 중 일부는 아직
  `participation_status='registered'`다. 자동 판정은 위험해서 안 했다.
  화면에서 직접 취소 처리해야 좌석이 반납된다. 현재 취소자 0명.

### 🟡 결정 대기

- 다음 행사(리더십 캠프)에 **하행이 여러 편으로 나뉘는지** → Phase 3 우선순위 좌우
- 캠퍼스 송금 등록 넛지 UI (총단 기록 1클릭 승인) — Phase 2-A에서 백엔드는 준비됐고
  화면은 Phase 5로 미뤄둔 상태

### 참고

- **리더십 캠프 전환은 Phase 3 이후에** 하는 게 안전합니다. `/admin/control`의
  "새 행사 시작"을 누르면 화면이 비워지지만, 지금 눌러도 되돌릴 수 있습니다
  (지난 행사 데이터는 삭제되지 않음).

---

## 6. 참조

- 코드: `~/Projects/carbus-web` · 운영: https://carbus-web.vercel.app
- Supabase: `qqtqwyhclscfjlefkiqr` (Northeast Asia)
- 백업: `~/Backups/carbus-web/` (PII 포함 — 로컬 전용)
- 검증 도구: `scripts/local-verify/` (README 참고)
- 개선안 목업: 이 작업 초기에 만든 아티팩트 (화면 7개 + 6단계 계획)

### 이 작업에서 나온 주요 발견

| 발견 | 실측 |
|---|---|
| 비고 사용률 | 599명 중 226명(37.7%) |
| 비고에서 "타지구"의 두 가지 뜻 | 소속 63건 / 이용수단 80건 (**정반대 의미**) |
| 감사로그 유령 이력 | 18,967건 중 73%가 실변경 없음 |
| 출석률이 100%에 못 닿던 이유 | 분모가 신청 기준(376)인데 분자는 배차 기준(372) |
| 환불 차액 | 46명 / 1,350,000원 (그중 28명은 아무 기록 없었음) |
| 하드 삭제로 사라진 수납 기록 | 10건 / 225,000원 |

---

## 7. 3-C 적대적 검증 결과 (2026-07-21)

8개 관점의 조사자를 서로 못 보게 병렬로 돌려 결함 후보 33건을 얻고, 같은 근본 원인끼리
병합해 19건으로 줄였다. 아래 A 는 수정 완료, B 는 남은 것이다.

### 7-A. ✅ 수정 완료 — blocker 1건 (커밋 `ab31181`)

**상행 배차 드롭다운이 항상 비어 있었다.** 신청 목록 화면에서 학우의 상행 호차를 바꾸는
드롭다운에 선택지가 하나도 안 뜬다(이미 배정된 사람은 자기 호차 1개, 미배정은 0개).
즉 **관리자가 화면에서 상행 수동 배차를 할 수 없었다.**

3-A 가 `buses.departure_slot_id` 를 `up_trip_id` 로 rename 했는데 `lib/admin/bus-options.ts`
만 옛 이름으로 남았다. 화면은 `up_trip_id` 를 넘기고 함수는 `departure_slot_id` 를 찾으니
비교가 항상 false 였다.

**왜 세 겹의 검증을 다 통과했는가** — 이게 이 사고의 진짜 교훈이다.

| 검증 | 왜 못 잡았나 |
|---|---|
| `tsc` | 타입이 `departure_slot_id?: number` 로 **선택 필드**였다. 없어도 오류가 아니다. |
| 단위 테스트 11개 | 픽스처가 옛 키를 갖고 있었다. 함수 단독으로는 정상 동작한다. |
| `pnpm build` | 런타임 값 문제라 빌드는 무관하다. |

**화면이 넘기는 실제 데이터와 테스트 데이터가 어긋난 것**이라, 단위 테스트로는 원리적으로
잡을 수 없는 종류였다. 수정에서 이 구멍 자체를 막았다:

1. `BusOption` 의 `up_trip_id` / `down_trip_id` 를 **필수 필드**로 바꿨다.
2. 화면의 `as BusInfo[]` **캐스트를 제거**했다. 캐스트가 있으면 Supabase select 문의
   컬럼 누락 검사가 통째로 무력화된다.
3. 검증: 일부러 select 에서 `down_trip_id` 를 빼보니 `tsc` 가 `TS2322 — Property
   'down_trip_id' is missing` 로 정확히 잡았다. 안전망이 실제로 작동한다.

같이 고친 것: 하행 드롭다운이 필터를 통째로 건너뛰어 전 호차를 보여주는데 3-C 가 서버에
"신청 편 = 차량 편" 검사를 넣어서, 화면에 뜬 호차를 고르면 서버가 거절하고 있었다.
상·하행 대칭 필터로 바꿔 서버 판정과 1:1 대응시켰다.

**운영 데이터 검증**: 로컬(운영 백업 재현)에서 기존 배차 상행 454건·하행 459건이
새 필터에서 **전부 편 일치, 불일치 0건**. 기존 배정을 하나도 막지 않는다.
옛 테스트가 경고했던 "하행 교차슬롯 86건"은 **상행 슬롯** 기준이고, 하행끼리는
전 차량이 같은 편(귀가 1편)이라 새 필터에 걸리지 않는다.

### 7-B. ✅ 수정 완료 — high 6건 (커밋 `7f0f7be`, 2026-07-27)

대부분 **"3-C 가 상행만 대칭화하고 하행을 빠뜨렸다"** 는 같은 뿌리다. 한 번에 고치는 게 싸다.
괄호 안은 몇 개 관점이 **독립적으로** 같은 걸 찾았는지 — 높을수록 실재 가능성이 크다.

1. **하행 차량순장·고정탑승 지정에 '편 일치' 검사가 없다 (4개 관점)** — `lib/admin/leaders.ts`
   의 `assertUpSlotMatch` 가 `mode !== "up"` 이면 즉시 통과시킨다. 하행 3시 차 순장으로
   지정했는데 그 사람이 6시 차 승객이어도 화면·서버·DB 어디서도 안 막고, 배차에서 조용히
   탈락한다. 후보 목록도 편을 안 보고, `buses/page.tsx` 가 `down_trip_id` 를 아예 안 가져온다.
2. **'마감 후 변동' 화면이 하행 편 불일치를 못 잡는다 (4개 관점)** —
   `app/admin/(protected)/changes/page.tsx:199-215` 의 배차 점검이 상행만 본다.
   재배차가 필요한데 목록에 안 뜬다.
3. **`CONTENT_FIELDS` 에 `up_trip_id`/`down_trip_id` 가 없다 (2개 관점)** — 같은 화면
   `:16-26`. 학우가 하행 편을 바꿔도 변동 목록에 **통째로 안 나타난다.**
4. **편성 화면(`/admin/trips`)이 하행을 3-C 이후로 갱신 안 했다 (2개 관점)** — 하행 편 변경
   잠금이 없는데 DB 가드는 막는다. 안내문이 현재 동작과 반대다.
5. **파생 트리거 구버전 분기가 기존 `down_trip_id` 를 무시하고 다시 정한다 (2개 관점)** —
   `20260721090000_registration_trips.sql:93-122`. 하행이 2편 이상이면 구버전 앱 경로에서
   편을 갈아끼우거나 저장을 전면 차단한다.
6. **CSV 의 '참석 유형' 열을 여전히 읽고 조용히 버린다** — `lib/csv/parse.ts:111-121`.
   옛 포맷 왕복 신청이 **편도 상행으로** 등록된다.

medium 9건·low 1건 중 위와 함께 고친 것:
- `post-load.sh` 가 `20260721070000` 을 재실행해 `d61f6b1` 의 `v_down_capacity` 수정을
  되돌리던 문제 → `20260721100000` 을 목록 끝에 붙여 다시 덮게 했다. **이건 검증 자체를
  무의미하게 만드는 종류였다** — 로컬이 배포될 형상과 다른 채로 "통과"가 나왔다.
- 취소자가 가리키는 편을 지울 때 raw FK 에러가 화면에 그대로 뜨던 것 → 문장으로 바꿨다
  (삭제 대신 비활성 안내).
- **하행 편 2개 이상 경로가 테스트에 한 줄도 없던 것** → 마이그레이션 자체검증 ④
  (구버전 경로가 기존 하행 편을 보존하는가) + `fleet-panel` 하행 잠금 테스트로 덮었다.

### 7-C. 검증 방법에 대한 메모

33건에 각각 심사자 3명을 붙이면 99명인데, 8개 관점이 독립으로 돌아 같은 결함을 최대 4번
중복 보고했다. **비싼 검증 단계 앞에는 반드시 병합 단계를 둘 것.** 그리고 blocker 를 실제로
확정한 것은 에이전트 의견이 아니라 **파일을 직접 열어 읽고 로컬 DB 에 SQL 을 날린 것**이었다.
운영 백업이 재현된 로컬 DB 가 있으면 그게 가장 강한 증거다 — 그걸 안 쓰고 의견을 더 모으는
것은 낭비다.

### 7-D. ✅ §7-B 를 고치다 나온 결함 3건 (커밋 `48c332c`, 2026-07-27)

같은 뿌리다. 그리고 **그중 2건은 blocker `ab31181` 과 정확히 같은 종류** —
화면이 보여주는 선택지가 서버가 허용하는 집합보다 넓다. 고르면 저장이 거부된다.

1. **배차 화면이 취소자를 대상 인원에 넣었다.** 엔진은 취소자를 빼고 도는데 이 화면만
   안 뺐다. 취소하면 배정이 반납되므로 취소자는 **미배정 목록에 영원히 남는다.**
   지금 취소자가 0명이라 안 드러났을 뿐이다.
2. **배차 화면의 수동 배정 드롭다운이 전 호차를 나열했다.** 신청 화면과 같은
   `busSelectOptions` 를 쓰게 해 서버 판정과 1:1 로 맞추고, 잔여석·신청 편을 함께 보인다.
3. **리더 관리 하행 드롭다운이 전 호차를 나열했다.** §7-B 1 을 고치기 전까지는 서버도
   하행을 안 봐서 우연히 맞았다. 서버만 고치고 이걸 안 고쳤으면 **새 막다른 길**이 됐다.

> 교훈: "서버 판정을 조인다"는 수정은 **그 판정을 먹이는 화면을 전부 같이 찾아야** 한다.
> 서버만 조이면 어제까지 되던 화면이 오늘 거부당한다. 이번엔 `up_trip_id` 로 전수 grep 해
> 세 화면(신청·배차·리더)을 찾았다.

### 7-E. 🔴 배포 직전 백업이 운행편을 흘리고 있었다 (커밋 `11928f9`)

백업을 **실제로 떠보다** 발견했다. 이 브랜치가 `backup-prod.mjs` 의 테이블 목록을
`event_trips` 로 바꿔놨는데, 백업을 뜨는 시점의 운영은 아직 `departure_slots` 다.
그래서 운행편 3행이 **통째로 빠진 채** `실패: 1` 한 줄만 찍고 **종료코드 0** 으로 끝났다.

하필 이 브랜치는 롤백 경로가 백업 복원뿐이다. 되돌릴 방법이 백업밖에 없는 배포에서
그 백업이 조용히 불완전할 수 있었다는 뜻이다.

수정: ① 이름이 바뀐 테이블은 옛 이름으로 재시도하고 찾은 이름 그대로 저장한다
(`load-backup.py` 의 `RENAMED_FROM` 이 그 이름으로 읽는다). ② 한 테이블이라도
못 뜨면 `exit 1`.

> 교훈: **테이블 이름을 바꾸는 마이그레이션은 백업 스크립트도 같이 본다.**
> 그리고 "백업 성공 확인"이 사람 눈에만 맡겨져 있으면 안 된다 — 종료코드로 만들어야
> `&&` 로 이어 쓸 수 있다.

---

## 8. Phase 4 — 행사 폴더화 (재설계, 2026-07-21)

### 8-A. 왜 이 작업이 생겼나

사용자 질문에서 출발했다. "행사 전환은 내가 할 수 있는 거 아니었나? 목업에서는 새 행사를
만들고 이전 행사는 폴더처럼 남아 있는 걸로 기억하는데."

조사 결과 **기억이 맞았다.** 과거 행사는 삭제되지 않고, `/admin/control` 의 "지난 행사"
목록에 "이 행사로 전환" 버튼이 실재하며(`components/admin/events-panel.tsx:96`),
`activate_event()` 는 `events` 두 줄만 UPDATE 하므로 몇 번을 오가도 데이터가 닳지 않는다.
`test-event-switch.sh` §9 가 왕복 복구를 실제로 검사한다.

**틀린 것은 비유였다.** 이건 폴더가 아니라 **한 번에 한 서랍만 열리는 캐비닛**이다.
전환은 `events.is_active` 라는 전역 플래그를 옮기는 것이고, 부분 유니크 인덱스가 동시 활성
1개를 DB 레벨에서 강제한다. 그래서 master 가 과거를 열어보는 순간 **순장들 화면까지 같이
과거로 간다.** 두 행사를 나란히 보는 것은 구조적으로 불가능하다.

여기서 실제 피해가 셋 나온다.

1. **차액 46명이 조용히 사라진다.** 리더십 캠프로 전환하면 `/admin/payments` 의 차액 카드가
   "0명"이 아니라 **카드 자체가 없어진다**(`components/admin/payments-panel.tsx:239` 의
   `balances.length > 0` 게이트). *정산이 다 끝난 상태*와 *행사가 바뀌어 안 보이는 상태*가
   화면상 구별되지 않는다. 에러도 안내도 없다.
2. **되돌려둔 사이 들어온 신청이 과거 행사로 저장된다.** `insertRegistration` 은 event_id 를
   안 넘기고(`lib/registrations/mutations.ts:24`) 컬럼 DEFAULT 가 `active_event_id()` 다.
   조회 불편이 아니라 데이터 오염이다.
3. **차량순장 담당 호차가 갈아엎어진다.** `create_event` 가 `profiles.driver_bus_id` 를
   행사 조건 없이 전역 재매핑하는데(`20260721070000_event_trips.sql:326-332`)
   `activate_event` 는 복구하지 않는다. 되돌려도 차량순장은 빈 화면에 갇힌다.
   "차량 이어받기"를 끄면 **전원 담당 호차가 NULL** 이 된다.

### 8-B. 좋은 소식 — 손댈 중심이 하나다

전역 결합이 **함수 하나로 수렴**한다: `active_event_id()`
(`20260721000000_events_core.sql:65-68`). RLS 8개·뷰 8개·컬럼 DEFAULT 8개·SECURITY DEFINER
함수 여럿이 전부 이 하나를 부른다. 폴더화의 핵심은 이 함수를 쪼개는 것이고 나머지는 부수 작업이다.

**배차 엔진은 0줄 바꾼다.** `lib/batch/engine.ts` 는 `.from()` 호출이 한 건도 없는 순수 함수다.
DB 준비도 끝나 있다 — `20260721050000_bus_batch_flags.sql:51-57` 이 배차 특례를 컬럼으로
승격하며 backfill 을 **전 행사에 걸쳐** 적용했고, 그 이유로 "지난 행사를 다시 활성화해
재배차할 수 있다"를 명시했다. 사용자 직관이 맞았다.

### 8-C. 설계 — 개념 두 개를 분리한다

지금 `events.is_active` 하나가 "신청이 들어가는 행사"와 "화면이 그리는 행사"를 겸직하고
있고, 이 겸직이 모든 사고의 뿌리다.

| 개념 | 무엇이 정하나 | 누가 바꾸나 |
|---|---|---|
| **진행 중 행사** (쓰기 대상) | `events.write_mode = 'live'` — DB 전역, 항상 정확히 1개 | master 가 행사를 넘길 때만 |
| **지금 보는 행사** (읽기 대상) | URL 경로 `/admin/e/<eventId>/...` | 관리자가 클릭할 때마다, **사람마다 따로** |

master 가 과거를 열어봐도 URL 만 바뀐다. 다른 사람 브라우저는 영향이 없고, 탭 두 개로
두 행사를 나란히 볼 수 있다.

**상태는 둘, 임시 예외가 하나.** `write_mode`: `live` | `closed`(기본). 끝난 행사는 읽기
전용이고, 정산·환불이 필요하면 master 가 사유를 남기고 **기본 60분, 최대 8시간** 잠금을
연다. `now()` 비교로 자동 재잠금되므로 cron 이 필요 없고, "풀어놓고 잊는" 가장 흔한 실패를
시간이 흡수한다. `is_active` 는 `write_mode` 를 따라가는 동기화 트리거로 남긴다 — 기존
코드가 계속 도는 안전판이자 롤백 여지다.

**주소창을 진실로 삼고, 헤더는 대조용으로만 쓴다.** 브라우저가 URL 의 eventId 를
`x-carbus-event` 헤더로 한 번 더 선언하고, DB 가 그 선언과 페이로드의 event_id 가 같은지
대조한다. 이게 없으면 "master 는 과거 화면을 보는데 저장은 진행 중 행사로 조용히 성공"하는
발견 불가능한 오배치가 남는다. **헤더는 권한을 주는 채널이 아니라 의도를 재확인하는
채널이다** — PostgREST 가 버전업으로 헤더를 안 넘겨도 시스템은 URL 기반 원안대로 동작한다.
최악의 경우가 "방어가 한 겹 준다"이지 "엉뚱한 행사에 쓰인다"가 아니다.

**뷰는 필터를 유지한다.** 뷰에서 행사 필터를 빼고 앱이 `.eq("event_id", ...)` 로 거르는 방식은
채택하지 않았다. 필터를 하나 빠뜨리면 그 화면에 전 행사가 합산되는데 **에러가 나지 않는다.**
정산 숫자가 조용히 틀리면 금전 사고이고, 롤백해도 그 숫자를 보고 집행한 송금은 안 돌아온다.
대신 뷰 정의 안의 `active_event_id()` 를 `viewing_event_id()` 로 **문자열 치환**하고 필터
구조는 그대로 둔다. 이러면 뷰가 여러 행사를 섞은 값을 낼 수 있는 상태 자체가 없다.

### 8-D. 쓰기 안전장치 5겹 — 전부 DB 에 둔다

전제는 **앱 코드는 언젠가 반드시 틀린다**는 것이다. "조심하면 된다"는 방어에 넣지 않았다.

1. **쓰기 상태 게이트** — 과거 행사는 잠금이 풀려 있지 않으면 어떤 역할도 못 쓴다.
   RESTRICTIVE 라 다른 모든 정책과 AND 로 묶여 우회 불가.
2. **진행 중 행사 유일성** — `unique on events(write_mode) where write_mode='live'`.
   두 행사가 동시에 신청을 받는 상태를 DB 가 만들 수 없다.
3. **열람·쓰기 대조** — 위 헤더 대조. 화면이 A 인데 페이로드가 B 면 거부.
4. **`guard_event_writable()` 트리거** — SECURITY DEFINER 함수는 RLS 를 통째로 우회하지만
   **테이블 트리거는 우회하지 못한다.** 8개 테이블에 `ENABLE ALWAYS` 로 붙인다. 검사 3가지:
   event_id NULL 금지 / **UPDATE 로 행이 행사를 갈아타는 것 금지**(요구 "과거 자료 보존"의
   유일한 물리적 보장 — RLS 는 행이 어디 속하는지만 볼 뿐 바뀌는 것을 못 본다) / 쓰기 가능
   여부.
5. **교차 행사 참조 가드** — 신청의 편·배차 호차, 차량의 순장·고정탑승이 다른 행사 행을
   가리키면 예외. `guard_bus_trip_scope()`(`20260721080000:198-230`)가 이미 같은 패턴을 쓴다.

**RLS 는 16개면 된다.** 테이블당 4개씩 32개로 쪼갤 필요가 없다. 현행이 이미 `for all` 에
`using`·`with check` 를 따로 주는 형태라, `using` 을 넓게(읽기 허용) `with check` 를
좁게(쓰기 제한) 재작성하면 8개로 끝난다. **다만 DELETE 는 별도 8개가 필요하다** —
PostgreSQL 은 DELETE 에 `with check` 를 적용하지 않고 `using` 만 본다. 이걸 안 메우면
master 가 과거 행사를 열어둔 채 삭제하면 과거가 지워진다.

⚠️ 정책 안의 함수 호출은 반드시 `(select public.writable_event_id())` 처럼 **서브쿼리로
감싼다.** plpgsql STABLE 함수는 인라인되지 않아, 안 감싸면 행마다 재평가한다.
`registration_audit` 18,967행에서 실측 차이가 난다.

### 8-진행 상황 (2026-07-27)

사용자 요청: **피드백 9건을 전부 반영해 완성한다.** 계획을 4단계로 다시 짰고
(§9), 그중 2단계가 이 행사 폴더화다. 설계는 §8-C~G 그대로 간다(사용자 확정).

**전 단계 완료 (2026-07-27).** 관리자 화면 주소는 이제 `/admin/e/<행사>/...` 이고,
사람마다·탭마다 다른 행사를 볼 수 있다.

| 단계 | 상태 | 커밋·비고 |
|---|---|---|
| 4-0 행사 이름 상시 표시 | ✅ | `a3e9956` — 관리자·임역원 헤더 양쪽 |
| 4-1 뷰 실물 대조 | ✅ | 뷰 10개 중 8개가 `active_event_id()` 호출, 전부 `security_invoker=on` |
| 4-2 `write_mode` + 동기화 + 잠금해제 RPC | ✅ | `a3e9956` |
| 4-3 앱이 `event_id` 명시 | ✅ | `51ca03d` — 쓰기 경로 6곳 |
| 4-4 DEFAULT 제거 + `guard_event_writable` | ✅ | `51ca03d` |
| 4-5 URL 에 eventId + 헤더 선언 + 네비 | ✅ | `b11d40a` — 라우트 15개 이동 |
| 4-6 읽기 확대(`viewing_event_id()`) | ✅ | `b11d40a` |
| 4-7 지난 행사 차량비 잠금 (§8-E) | ✅ | 요금 변경 = 청구액 재계산 = 금전 사고 |

**§8-E 처리 결과**
- ✅ `sync_role_labels` 행사 범위 — **지금도 오염 중이던 것**
- ✅ `campus_remit_add` event_id 명시
- ✅ `payments.ts` upsert — DEFAULT 제거 시 금전 사고였을 것
- ✅ `update_event_fares` 쓰기 가드
- ✅ `log_registration_change` — 감사 이력이 자기 행사를 따라가게 (이것도 잠재 버그였다)

**의도적으로 미룬 것 — 지금 구조에서 왜 위험하지 않은가**
- `leaders.ts` 의 무범위 UPDATE: RLS 가 여전히 **한 번에 한 행사**(보는 행사)로 좁히므로
  다른 행사 행에 닿지 않는다. §8-E 의 우려는 "읽기가 전 행사로 열리는" 설계 기준이었는데,
  채택한 설계는 그게 아니다.
- `profiles.driver_bus_id` 조인 테이블화: 차량순장이 행사마다 다른 호차를 맡게 되면 필요.
  지금은 `/driver` 라우팅 판정에만 쓰인다.
- `system_config` 행사별 분해: 지금은 싱글턴이라 "여름수련회는 정산 단계, 리더십 캠프는
  신청 단계"가 **동시에 성립하지 못한다.** 두 행사를 정말 병행 운영할 때 필요하다.

**되돌리기.** `20260727040000`(4-6) 하나만 되돌리면 읽기가 다시 진행 중 행사로 좁아지고
앞 단계는 그대로 정상 동작한다. 그래서 이걸 마지막에 뒀다.

**작업 중 검사가 실제로 잡은 것 두 개** (둘 다 조용히 지나갈 수 있었다)
1. 백업을 복원하면 행사가 `is_active=true` 인데 `write_mode='closed'` 가 되어
   **화면은 정상인데 모든 쓰기가 막히는** 상태. 원인은 replica 모드에서 동기화 트리거가
   꺼지는 것 — `trg_reg_000_derive` 와 똑같은 함정이다.
2. 옛 backfill 의 `enable trigger user` 한 줄이 **쓰기 가드 8개를 ORIGIN 으로 내려놓은 것.**
   방어선이 사라진 줄도 모를 뻔했다. post-load 에 복구 단계 + 검사를 넣었다.

### 8-E. 같은 마이그레이션에서 반드시 함께 고칠 것

미루면 게이트를 켜는 순간 깨지는 것들이다.

- **`sync_role_labels()`** (`20260520020000_initial_schema.sql:463-478`) — event_id 조건이
  없어 라벨 하나를 고치면 과거 행사 신청자의 `roles[]` 까지 바뀌고 감사 이력도 오염된다.
  **오늘도 진행 중인 오염이다.** 게이트를 켜면 이 트리거가 스스로 막혀 라벨 관리 화면이
  통째로 깨진다.
- **`campus_remit_add`** (`20260520140000:53-68`) — event_id 미명시. 자매 함수
  `campus_remit_delete` 는 `20260721010000` 에서 고쳐졌는데 add 만 빠졌다. 한 줄이다.
- **`lib/admin/payments.ts:26-29`** — "event_id 는 컬럼 기본값이 채우므로 페이로드에 안 넣는다"
  고 주석에 명시돼 있고 `onConflict: "event_id,campus_id"` 를 쓴다. DEFAULT 를 제거하면
  이 upsert 가 깨진다. **정산 데이터라 금전 사고다.**
- **`lib/admin/leaders.ts:86-99`** — `clearDriverEverywhere` 가 호차 id 도 행사도 조건 없이
  UPDATE 하고, `clearFixedEverywhere` 는 조건 없이 전체 호차를 읽어 루프를 돈다.
  지금 안전한 유일한 이유가 RESTRICTIVE RLS 다. **읽기를 넓히는 순간 가장 먼저 깨진다.**
- **§8-A 의 결함 3개** — `profiles.driver_bus_id` 를 `(profile_id, event_id, bus_id)` 조인
  테이블로 승격, `system_config` 를 행사별로 분해(지금 `CHECK (id=1)` 싱글턴이라
  "여름수련회는 정산 단계, 리더십 캠프는 신청 단계"가 성립 못 한다), `update_event_fares` 에
  활성 행사 검사 추가.

### 8-F. 단계 — 앱을 먼저, 읽기 확대는 맨 마지막

**대원칙.** 읽기를 먼저 넓히면 앱은 아직 필터가 없어 여러 행사가 합산돼 보인다. 반대 순서면
앱이 event_id 를 명시해도 DB 는 여전히 진행 중 행사만 허용하므로 **결과가 지금과 완전히
같다** — 즉 앱 변경을 무위험으로 검증할 수 있다. 각 단계는 그 자체로 배포 가능하고
중간에 멈춰도 앱이 정상 동작해야 한다.

| 단계 | 내용 | 규모 |
|---|---|---|
| **4-0** | **행사 이름 상시 표시** — 관리자 헤더·순장 화면 상단에 현재 행사 이름. DB 변경 없음. | 30분 |
| 4-1 | 운영 실물 대조 (`pg_get_viewdef` 덤프, 뷰 개수·정의 확정) | 읽기만 |
| 4-2 | `write_mode` 도입 + `is_active` 동기화 트리거 + 잠금/해제 RPC | 마이그레이션 1 |
| 4-3 | 앱이 event_id 를 **명시**하게 (DEFAULT 는 아직 유지) | 앱 다수 |
| 4-4 | DEFAULT 제거 + `guard_event_writable` 트리거 + 교차 참조 가드 | 마이그레이션 1~2 |
| 4-5 | URL 에 eventId + 헤더 선언 + 네비 15개 | 앱 다수 |
| 4-6 | **읽기 확대** — `viewing_event_id()` 로 뷰·RLS 치환 (되돌리기 가장 쉬운 마지막) | 마이그레이션 1~2 |

**4-0 을 왜 맨 앞에 두나.** 지금 행사 이름이 보이는 화면은 `/admin/control` 하나뿐이고
순장 화면에는 0건이다. 전역 전환이 일어나도 **아무도 그걸 모른다.** 이 결여가 현재 오염
위험의 실제 원인이라, 어떤 설계로 가든 버려지지 않는 작업이면서 당장 위험을 줄인다.

**규모 추정.** DB 마이그레이션 4~6개. 앱은 행사 범위 데이터를 참조하는 파일이 실측 52개인데,
그중 supabase 클라이언트를 만드는 곳이 46개다. **헤더 주입 방식이면 `lib/supabase/` 3개
파일만 고치고 나머지는 그대로 둘 수 있고, 모든 쿼리에 `.eq()` 를 붙이는 방식이면 52개를
전부 손대야 한다. 이 선택이 작업량을 3 대 52 로 가른다 — 착수 전에 반드시 정할 것.**

### 8-G. 하지 말아야 할 것

- **뷰에서 행사 필터를 빼고 앱 필터에 맡기지 마라.** 빠뜨려도 에러가 안 나고 숫자만 틀린다.
- **읽기 확대를 먼저 하지 마라.** 4-6 이 마지막인 이유다.
- **`pg_get_viewdef` 없이 마이그레이션 파일만 보고 뷰를 고치지 마라.** `v_campus_stats` 는
  이미 3회 재정의됐다. §4 규칙 2 와 `20260721100000:48-49` 가 같은 경고를 남겼다.
- **`security_invoker = on` 누락 검사 DO 블록을 빼지 마라.** 누락 시 뷰가 소유자 권한으로
  돌아 RLS 가 통째로 우회된다. 실측 피해가 "campus_admin 이 8건 대신 599건 조회"로 기록돼 있다.
- **`archive_reader` 같은 전용 role 로 과거를 읽는 설계를 쓰지 마라.** 검토했고 **작동하지
  않는다.** RESTRICTIVE 정책 8개에 `TO` 절이 없어 `TO PUBLIC` 이고, 그 role 은 owner 도
  BYPASSRLS 도 아니라 과거 행사 조회가 **항상 0행**이 된다.
- **앞으로 쓰는 모든 마이그레이션 끝에 `pg_trigger.tgenabled` 검사를 붙여라.**
  `guard_event_writable` 은 backfill 때문에 꺼질 수 있고, 꺼진 채 남으면 방어선이 통째로 없다.

---

## 9. 사용자 피드백 9건 — 재계획 (2026-07-27)

Phase 3 배포 후 사용자가 물었다: "예전에 준 피드백이 지금 버전에 다 반영됐나?
목업도 반영됐나?" **대조 결과 아니었다.** 그래서 계획을 다시 짰다.

### 9-A. 대조 결과 (배포된 코드 기준 실측)

| 피드백 | 상태 | 근거 |
|---|---|---|
| 상/하행 버스·출발시각 자유 선택 | ✅ Phase 3 | `/admin/trips` |
| 초기화 버튼 | ✅ Phase 1 | `/admin/control` "새 행사 시작"(비파괴) |
| 전체 순장/순원 수정 폼이 맨 위 | ✅ 1단계 | 그 사람 행 아래에서 펼침 |
| 출석 호차별 이동 | ✅ 1단계 | 상단 고정 칩 + 진행률 |
| 로그 50건 제한 | ✅ 1단계 | 전체 이력 + 검색·필터·페이지 |
| 변경 이력 추적 | ✅ 1단계 | 이름 클릭 → 그 사람 이력만 |
| 비고 구조화 (타지구·KTX·확정대기) | ✅ 3단계 | `transport_legs` — 방향별 이동수단 + 확정 대기 |
| 부분참 정보 산발 | ✅ 4단계 | 캠퍼스 카드 32개 → 한 표 + 필터 |
| 캠퍼스 송금 등록 안 함 | ✅ 4단계 | 임역원 배너 + **총단 1클릭 대리 등록** |

> 마지막 줄이 핵심이다. 기능을 만들었는데 **원래 문제는 하나도 안 풀렸다**는 게
> 데이터로 증명된다. 등록을 유도하는 화면이 없어서다.

### 9-B. 목업

레포에 있는 건 [design/brief.md](design/brief.md) 뿐이고 이건 목업을 **만들기 위한
주문서**다. 목업 7장은 claude.ai 아티팩트로만 있고 코드로 옮긴 적이 없다.
게다가 **지금은 낡았다** — 브리프의 화면 목록에 `/admin/trips`·`/admin/changes`·
`/admin/logs`·`/admin/control`·`/admin/partial` 이 없다. 그대로 옮기면 화면 절반이 빠진다.
→ **4단계에서 지금 있는 화면 전부 + 폴더화된 네비를 반영해 다시 그린다.**

### 9-C. 4단계 계획 — 의존성으로 정한 순서

폴더화가 모든 화면 상단의 네비게이션을 바꾸므로, 화면 정보 구조를 먼저 손대면
두 번 일한다. 반대로 아래 1단계 항목들은 폴더화와 무관해 지금 해도 안 버려진다.

1. **즉시 불편 해소** ✅ 배포됨 — 로그·수정폼·출석 이동
2. **행사 폴더화** ✅ 배포됨 — §8 진행 상황 표 참고
3. **비고 구조화** ✅ 배포됨 — `transport_legs`
4. **부분참 재설계 + 송금 유도 + 3-D 환불 감지** ✅ 배포됨

**피드백 9건 전부 반영 완료 (2026-07-28).** 남은 것은 목업 갱신·화면 응집(§9-D).

> 원래 계획은 Phase 6 하나가 피드백 9건 중 5건을 지고 있었다. 그러면 "나머지 전부"
> 단계가 되어 영원히 안 끝난다. 성격이 다른 둘(작고 독립적인 것 / 정보 구조를
> 다시 짜야 하는 것)을 갈라서 앞뒤로 나눴다.


---

## 10. 야간 작업 결과 (2026-07-28)

사용자 지시로 자는 동안 3·4단계를 진행해 **피드백 9건을 전부 반영·배포**했다.

### 10-A. 무엇이 올라갔나

| 마이그레이션 | 내용 |
|---|---|
| `20260728010000` | `transport_legs` — 방향별 이동수단(우리버스/타지구+지구/KTX/자차/기타) + 확정 대기 |
| `20260728020000` | 전체 지구 명단 53개 (사용자 제공). **기존 행은 안 건드림** |
| `20260728030000` | `master_remit_add` — 총단 대리 송금 등록 |
| `20260728040000` | **보안** — 역할 검사 NULL 통과 + anon 실행 권한 회수 |
| `20260728045000`·`047000` | 헤더 읽기 안전화 (`request_event_header`) |
| `20260728050000` | 3-D 환불 감지 (`refund_due` · `fee_now`) |

### 10-B. 🔴 결정 대기 — 기존 소속 5건을 어떻게 할까

공식 지구 명단을 넣으면서 **기존 `org_units` 5건이 명단에 없는 것**이 드러났다.
이미 실제 인원의 소속이 걸려 있어 **자동으로 합치지 않고 그대로 뒀다.**

| 기존 이름 | 걸린 인원 | 공식 명단 후보 |
|---|---|---|
| 서울지구 | **10명** | 서울동1/동2/서/서2/남/북/북동/북동2/중앙/북중앙 중 **어디인지 사람만 안다** |
| 순수지구 | 2명 | 순천여수? (별칭에 "순천지구") |
| 김천구미지구 | 1명 | 김천구미 |
| BI 선교부 | 1명 | 외국인사역부 B.I. |
| 평안지구 | 0명 | 평택안성? (인원 0이라 지워도 안전) |

→ 정해 주시면 합치는 마이그레이션을 씁니다. 그때까지는 드롭다운에 양쪽이 다 보입니다.

### 10-C. 작업 중 잡은 결함 — 전부 조용히 지나갈 수 있던 것들

1. **역할 검사가 미로그인에서 통과.** `current_role()` 은 NULL 을 돌려주는데
   `NULL <> 'master'` 는 참이 아니라 NULL 이라 IF 가 거짓으로 본다. 게다가 PostgreSQL 이
   새 함수 EXECUTE 를 PUBLIC 에 기본 부여해 **anon 이 호출 가능**했다 —
   지난 행사 잠금 열기·임의 송금 기록 생성 등. 가드를 `is distinct from` 으로 바꾸고
   권한을 회수했다. **앞으로 역할 검사는 반드시 `is distinct from` 을 쓸 것.**
2. **쓰기 가드 트리거가 헤더를 무방비 캐스팅.** 헤더가 비거나 JSON 이 아니면 8개
   테이블의 모든 쓰기가 실패한다. 같은 캐스팅을 두 곳에 복사해둔 게 원인이라
   `request_event_header()` 하나로 모았다.
3. **SQL 함수는 CASE 로 캐스팅을 못 막는다.** 인라인되며 계획 단계에서 미리 평가돼
   "during startup" 에러가 난다. 방어가 필요한 곳은 plpgsql 로 쓸 것.
4. **`registrations.fee` 는 생성 컬럼이 아니다.** 납부 시점에 얼어 있어서, 3-D 의 첫
   공식(`낸 돈 − fee`)이 **아무것도 못 잡았다** — 고치려던 버그를 그대로 재현했다.
   자체검증이 잡았다. 지금 참여 형태로 다시 계산한 `fee_now` 와 비교해야 한다.
5. **배포 명령의 종료코드 함정.** `node backup.mjs | tail && db push` 에서 파이프의
   종료 코드는 `tail` 것이라 백업 실패가 삼켜진다. **`set -o pipefail` 필수**(§3).

### 10-D. 남은 것

- **목업 갱신 + 화면 응집** — 목업 7장은 코드로 옮긴 적이 없고 지금은 낡았다(§9-B).
  폴더화로 네비게이션이 바뀌었으니 지금이 다시 그릴 시점이다.
- **관리자 화면 육안 확인** — 로그인이 필요해 자동 검증이 안 되는 부분.
- 옛 컬럼(`departure_slot_id`·`uses_return_bus`) 제거는 운영 안정 후.

---

## 11. 다음 세션 시작점 (2026-07-28 야간 작업 종료 시점)

### 11-A. 지금 상태 — 배포 완료 + 다음 작업 결정까지 확정

**다음 세션은 §11-C 구현 지침을 그대로 따라 바로 작업을 시작하면 됩니다.**
결정을 다시 받을 필요 없습니다 — 4가지 전부 2026-07-28 에 확정됐습니다.


**피드백 9건 전부 반영·배포 완료.** 그 위에 행사 폴더화·편성 화면·환불 감지까지
올라가 있습니다. 마이그레이션은 `20260728060000` 까지 운영에 적용됐고,
코드는 `master` 가 Vercel 에 배포돼 있습니다.

검증 상태: 마이그레이션 자체검증 전건(운영에서도) 통과 · 빈 DB 전량 재적용 ·
운영 백업 재현 기준선 일치(신청 599 · 감사 18,967 · 장부 1,081 · 배차 454/459 · 차액 46명).

### 11-B. ✅ 목업 결정 4가지 — **확정됨 (2026-07-28)**

동규님이 **이전 목업(0cf72c05)의 디자인**을 선택하셨고, 그 디자인으로 다시 그린
**목업 v2: https://claude.ai/code/artifact/a71287a8-5c94-4ff1-b64a-5096a2d9bb6e**

그 문서의 결정 4가지에 **답이 나왔습니다. 다음 세션은 바로 구현하면 됩니다.**

| | 결정 | 무엇을 해야 하나 |
|---|---|---|
| Q1 | **오른쪽 서랍으로 간다** | 지금의 "행 아래 펼침"을 서랍으로 교체 |
| Q2 | **이전 목업대로 — "맞나요?" 승인 방식** | 임역원이 금액을 계산·입력하지 않고 확인만 |
| Q3 | **타지구 확정 시 좌석 자동 반납** | 확정을 누르면 그 방향 편을 자동으로 비운다 |
| Q4 | **수송 요청 만든다** — 단, **픽업 장소를 일반 필드로** | "평창역" 같은 지명을 박지 말 것 |

### 11-C. 구현 지침 — 결정별로

#### A. 편집 서랍 (Q1)

`components/admin/registrations-panel.tsx` 의 `renderRow` 가 지금 행 아래에
`<tr><td colSpan>` 으로 폼을 펼칩니다. 이걸 오른쪽 서랍으로 바꿉니다.

- **표를 밀지 않고 스크롤도 움직이지 않아야** 합니다 — 그게 서랍을 고른 이유입니다.
  (맨 위로 올라가는 문제는 지금도 해결돼 있지만, 폼이 열리면 아래 행들이 밀립니다)
- 레이아웃: 목업 v2 화면 2의 `.split` — 왼쪽 표 + 오른쪽 300px 서랍, 서랍 왼쪽에 액센트 테두리.
- 이전 목업은 **"필드마다 즉시 저장 · 저장 버튼 없음"** 입니다. 임역원 그리드
  (`lib/registrations/mutations.ts` 의 `updateCells`)에 **필드 단위 저장 + 충돌 감지가
  이미 있습니다** — 그걸 재사용하는 게 맞습니다. 지금 admin 폼은
  `updateRegistrationFields` 로 통째 저장이라 다릅니다.
- 서랍 안에 들어갈 것: 참여 상태 · 상행/하행 편 · 납부 · **이동수단(방향별)** · 비고.
  이동수단 입력은 `components/admin/transport-picker.tsx` 를 그대로 옮기면 됩니다.

#### B. 송금 "맞나요?" 승인 (Q2)

`components/campus/payments-panel.tsx`.

- **총단이 기록한 입금액**(`campus_payment_settlements.master_received_total`)을 임역원
  화면에서 읽어, **"총단은 N원 받았다고 기록했어요. 맞나요?"** 카드로 띄웁니다.
  → `맞아요` 누르면 그 금액으로 송금 기록 생성 · `금액이 달라요` 누르면 지금의 직접 입력으로.
- **RPC 는 새로 만들 필요 없습니다.** 기존 `campus_remit_add(p_amount, p_note)` 를
  그대로 쓰되 note 를 `'총단 기록 확인'` 으로 넣으면 누가 어떻게 등록했는지 남습니다.
- 총단이 아직 기록을 안 했으면(=`master_received_total` 이 0/NULL) 이 카드는 안 뜨고
  지금의 배너가 그대로 나옵니다. **두 경로를 다 남기세요** — 승인식은 총단이 먼저
  기록해야만 성립합니다.
- 지금 배포된 **총단 1클릭 대리등록**(`masterRemitFor`)은 그대로 둡니다. 같은 문제의 반대편입니다.

#### C. 타지구 확정 → 좌석 자동 반납 (Q3)

**DB 트리거로 하세요.** 앱에서 하면 PostgREST 직접 호출로 우회됩니다
(이 레포가 반복해서 배운 것 — §8-D).

- `transport_legs` 의 `status` 가 `pending` → `confirmed` 로 바뀌고 `mode='other_district'`
  이면, 그 방향의 `registrations.up_trip_id` 또는 `down_trip_id` 를 **NULL 로** 만듭니다.
- **그 방향의 배정 호차(`assigned_*_bus_id`)도 같이 비워야** 실제로 좌석이 반납됩니다.
  편만 비우면 배정이 유령으로 남습니다.
- ⚠️ **부수 효과가 하나 있는데 좋은 것입니다.** 편이 비면 `attendance_type` 이 바뀌고,
  그러면 `v_payment_balance.fee_now` 가 줄어 **환불 대상 목록에 자동으로 뜹니다.**
  낸 사람이 타지구로 확정되면 돌려줄 돈이 생기는 게 맞으므로 의도한 동작입니다.
- 화면에서는 **확인 대화상자**를 띄우세요. 되돌리려면 재배차해야 합니다
  (동규님이 이 위험을 알고 자동을 택하셨습니다).
- 자체검증 필수: 확정 전 좌석 유지 → 확정 후 편·배정 모두 NULL → 환불 대상에 등장.

#### D. 수송 요청 + 참여기간 (Q4) — **새 필드가 필요한 유일한 항목**

> ⚠️ **동규님 지시: 픽업 장소에 특정 지명을 박지 말 것.**
> "이번에는 평창역이지만 앞으로는 픽업 장소가 달라질 수 있다."
> → `place` 를 **자유 입력 텍스트**로 두고, 같은 행사에서 이미 쓰인 장소를
> **자동완성 후보**로 보여주면 하드코딩 없이 표기가 통일됩니다.
> 장소 마스터 테이블을 만들지 마세요 — 행사마다 달라지는 것에 마스터를 두면 관리 부담만 늘어납니다.

새 테이블 제안:

```sql
create table pickup_requests (
  id              bigint generated always as identity primary key,
  event_id        uuid not null references events(id),
  registration_id uuid not null references registrations(id) on delete cascade,
  pickup_at       timestamptz,          -- NULL 허용 = "시각 미정" (보드에서 빨강으로 모임)
  place           text,                 -- 자유 입력. 지명을 코드에 박지 않는다
  direction       text check (direction in ('up','down')),
  note            text,
  ...
);
```

- **행사 범위 3종 세트를 반드시 붙일 것** (§10-C 6번): `event_scope` 정책 ·
  `guard_event_writable` 트리거 `ENABLE ALWAYS` · 교차 행사 참조 가드.
  그리고 `backup-prod.mjs` · `load-backup.py` · `post-load.sh` 세 곳에 등록.
- **보드**(목업 v2 화면 5 하단): `(날짜, 시각, 장소)` 로 묶어 그대로 간사 차량 배차표가 됩니다.
  `pickup_at` 이 NULL 인 사람은 **"시각 미정"** 묶음으로 따로 모아 빨강으로 — 그게 곧 할 일입니다.
- **참여기간**은 별개 필드입니다: `registrations.attend_from` · `attend_to` (date).
  부분참이 "며칠부터 며칠까지"인지 지금은 비고에 글로 적힙니다.

#### E. 외부수단 확정 관리 화면 — 위 C 와 함께

`/admin/e/<행사>/transport` (목업 v2 화면 6). **데이터는 `transport_legs` 에 이미 다 있습니다.**

- 지구별 묶음 · 경과일(`created_at` 기준) · 일괄 확정 · **잡아둔 좌석 수** 집계.
- 일괄 확정이 곧 위 C 의 자동 반납을 대량으로 트리거합니다 — 확인 대화상자에 몇 석이
  반납되는지 숫자로 보여주세요.
- **사람별 변경 타임라인**(목업 v2 화면 9)도 같은 데이터라 함께 만드는 게 쌉니다.
  감사 이력(`v_registration_changes`)을 세로로 세우고 경과일을 붙이면 됩니다.

#### 작업 순서 제안

1. **C + E** (자동 반납 + 확정 관리 화면) — 서로 맞물려 있고, 데이터가 이미 있어 가장 빠릅니다. 1.5~2일
2. **A** (편집 서랍) — 독립적. 0.5~1일
3. **B** (송금 승인) — 독립적. 0.5일
4. **D** (수송 요청 + 참여기간) — 새 필드가 필요해 마지막. 2~3일

### 11-D. 🔴 운영자가 직접 해야 하는 것 (코드 아님)

1. **행사 전환** — 리더십 캠프. 정산이 끝났고 폴더화가 됐으니 전환해도 여름수련회
   화면이 사라지지 않습니다. `/admin/control` → "새 행사 시작".
2. **관리자 화면 육안 확인** — 로그인이 필요해 CC 가 자동 검증할 수 없는 부분.
   (이동수단 입력은 2026-07-28 동규님이 확인 완료)
3. **실전 리허설 권장** — 캠프 신청을 열기 전에 새 행사에서 입력→편성→배차→출석→정산을
   한 번 돌려보기. 이번에 바뀐 게 많습니다.

### 11-E. 미뤄둔 기술 부채

- **옛 컬럼 제거** (`departure_slot_id` · `uses_return_bus`) — 파생 트리거가 동기화 중.
  운영이 며칠 안정된 뒤에. **지우는 순간 되돌릴 여지가 사라집니다.**
- **`system_config` 행사별 분해** — 지금 싱글턴이라 "수련회는 정산 단계, 캠프는 신청 단계"가
  **동시에 성립하지 못합니다.** 두 행사를 정말 병행 운영하면 필요.
- **`profiles.driver_bus_id` 조인 테이블화** — 차량순장이 행사마다 다른 호차를 맡으면 필요.
- **v2.0.0 태그** — 동규님이 "4·5·6 다 끝난 뒤"라고 하셨고 그 내용은 사실상 다 들어갔습니다.
  목업 반영까지 하고 붙일지, 지금 붙일지는 동규님 결정.

### 11-F. 이 세션에서 배운 것 — 반드시 지킬 것

1. **역할 검사는 `is distinct from`.** `current_role() <> 'master'` 는 미로그인(NULL)에서
   **통과합니다.** 그리고 새 함수는 EXECUTE 가 PUBLIC 기본이라 anon 이 부를 수 있습니다.
   → 권한 함수를 만들면 `revoke ... from public` + `grant ... to authenticated` 필수.
2. **`request.headers` 를 직접 캐스팅하지 마라.** `request_event_header()` 를 쓸 것.
   무방비 캐스팅이 8개 테이블의 모든 쓰기를 멈출 수 있습니다.
3. **방어가 필요한 함수는 plpgsql.** SQL 함수는 인라인되며 계획 단계에서 식이 미리
   평가돼 CASE 보호가 통하지 않습니다("during startup" 에러).
4. **`registrations.fee` 는 생성 컬럼이 아니라 납부 시점 동결값.** 지금 청구액이 필요하면
   `v_payment_balance.fee_now` 를 쓸 것.
5. **배포는 `set -o pipefail`.** `cmd | tail && push` 는 백업 실패를 삼킵니다.
6. **새 테이블을 만들면** `backup-prod.mjs` · `load-backup.py` · `post-load.sh`(ENABLE ALWAYS
   복구 목록) **세 곳 모두** 등록할 것.

---

## 12. C + E 완료 — 타지구 확정 시 좌석 자동 반납 + 확정 관리 화면 (2026-07-28)

§11-C 의 작업 순서 1번입니다. **로컬 검증까지 끝났고 아직 배포 전입니다.**

### 12-A. 무엇이 달라지나 (한 문장)

타지구 차량이 확정되면 그 사람이 잡고 있던 **우리 버스 자리가 그 자리에서 반납**되고,
확정을 기다리는 사람들이 지구별로 한 화면에 모입니다.

지금까지는 "확정 대기 중이니 좌석은 잡아둔다"는 규칙만 있고 **놓는 쪽이 없었습니다.**
확정이 나도 그 사실이 어디에도 모이지 않아 아무도 편을 비우지 않았고, 결과는
빈 좌석을 태우고 출발하는 것이었습니다.

### 12-B. 들어간 것

**마이그레이션 `20260728070000_transport_confirm_release.sql`**

1. `release_seat_on_transport_confirm()` + `trg_transport_legs_zz_release` (AFTER, ENABLE ALWAYS)
   — `mode='other_district'` 이고 `status='confirmed'` 이면 그 방향의 `*_trip_id` 와
   `assigned_*_bus_id` 를 **둘 다** NULL 로. 편만 비우면 배정이 유령으로 남습니다.
   INSERT 도 대상입니다(처음부터 확정으로 입력하는 경우). 이미 비어 있으면 `where` 절이
   걸러서 감사 로그를 더럽히지 않습니다.
2. **`guard_assignment_columns()` 개정** — 이게 없으면 임역원이 확정을 눌렀을 때
   그 자리에서 막힙니다. SECURITY DEFINER 는 RLS 는 우회해도 트리거는 못 우회합니다.
   우회 플래그(GUC) 대신 **가드가 `transport_legs` 를 직접 보고** 판단하게 했습니다 —
   이 문을 열려면 실제로 그 방향에 확정된 타지구 이용을 등록해야 하는데, 그 상태에서는
   좌석 반납이 애초에 정당합니다. **바뀐 방향마다 따로 봅니다**(한 방향이 정당하다고
   다른 방향까지 열어주면 "하행은 타지구 확정, 상행은 슬쩍 재배차"가 통과합니다).
3. `v_transport_legs_detail` 뷰 (`security_invoker = on`) — 화면용. `held_trip_id` /
   `held_bus_id` 가 "지금 좌석을 잡고 있는가", `days_waiting` 이 경과일입니다.

**화면**

- `/admin/e/<행사>/transport` (**신규**, master·viewer). nav 에 "이동수단" 추가.
  지구별 묶음 · 경과일 · 잡아둔 좌석 수 · **일괄 확정**(master 만).
  확인 대화상자에 **몇 석이 반납되는지 숫자로** 나옵니다.
- **모순 상태 섹션** — "확정인데 아직 좌석을 잡고 있는" 사람. 확정을 먼저 등록하고
  나중에 편을 지정하면 생깁니다. **트리거로 막지 않았습니다** — 편 드롭다운은
  이동수단을 모르므로, 막으면 "화면 선택지 ⊋ 서버 허용 집합" 결함이 또 나옵니다(§7-A).
  막는 대신 보여줍니다.
- `reg-form` 저장 시 확인 대화상자 + `transport-picker` 안내 문구 갱신
  (옛 문구가 "확정되면 운행편을 비워 주세요"였는데 이제 자동입니다).
- `lib/admin/transport.ts` 에 `confirmLegs(ids)` — 이미 확정된 건 건드리지 않습니다.

**스크립트**: `post-load.sh` 의 ENABLE ALWAYS 복구 목록 + 검사에 새 트리거 등록(§11-F 6).
새 테이블은 없으므로 `backup-prod.mjs` · `load-backup.py` 는 손대지 않았습니다.

### 12-C. 검증 (전건 통과)

- 마이그레이션 자체검증 8건: 대기 중 좌석 유지 → 확정 시 편·배정 **둘 다** 반납 →
  반대 방향 보존 → 참여형태 왕복→편도 → 환불 대상 등장(실측 25,000원) → 뷰 반영,
  그리고 **임역원 경로 2건**(직접 배차 변경은 여전히 차단 / 타지구 확정으로는 통과).
  임역원 검증은 `request.jwt.claims` 의 sub 만 바꿔 `reset role` 함정을 피했습니다.
- 스냅샷 대조: 의도한 변경만(뷰 1개 · 트리거 1개). 데이터 변화 0.
- `test-cancel` · `test-ledger` · `test-event-switch` PASS.
- 빈 DB 전량 재적용 → 백업 재적재 → **기준선 완전 일치**
  (신청 599 · 감사 18,967 · 소속 65 · 장부 1,081 · 차액 46 · 배차 454/459).
- 운영 실데이터 예행(롤백): 대기 7건 3개 지구 → 한 지구 일괄 확정 → 대기 4건 ·
  모순 0 · **환불 대상 46명 → 49명**(확정된 3명이 자동 등장 — 의도한 부수 효과).
- 게이트: tsc 0 · eslint 0 · **테스트 144** · build(라우트에 `/transport` 추가).

### 12-D. 배포할 때

DB 먼저, 코드 나중입니다(§3). 새 컬럼이 없어 **코드가 먼저 올라가도 화면만 비고
데이터는 안 깨집니다** — phase3-trips 같은 rename 이 아니라서 순서 위험이 낮습니다.

```bash
set -o pipefail
node scripts/local-verify/backup-prod.mjs ~/Backups/carbus-web/$(date +%Y-%m-%d_%H%M)-pre-transport \
  && supabase db push
```

### 12-E. 남은 것 — §11-C 의 A · B · D

순서 그대로 A(편집 서랍) → B(송금 승인) → D(수송 요청 + 참여기간).

---

## 13. A 완료 — 편집 서랍 (2026-07-28)

§11-C 의 작업 순서 2번. **로컬 검증까지 끝났고 아직 배포 전입니다.**

### 13-A. 무엇이 달라지나

`/admin/e/<행사>/registrations` 에서 연필을 누르면 **표 오른쪽에 서랍**이 열립니다.
표는 제자리에 있고 스크롤도 움직이지 않습니다.

예전에는 그 사람 **행 아래에** 폼을 펼쳤습니다. "화면 맨 위로 되돌아가야 한다"는
문제는 그걸로 풀렸지만, 폼이 열릴 때마다 **아래 행들이 통째로 밀려서** 방금 보던
자리가 사라졌습니다. 서랍은 그 두 문제를 동시에 없앱니다.

**저장 버튼이 없습니다.** 칸을 고치면 그 칸만 바로 저장됩니다.
(고르는 칸은 고르는 즉시, 글자 칸은 칸을 벗어날 때)

### 13-B. 어떻게 만들었나

- **`updateCells` 재사용** (`lib/registrations/mutations.ts`) — 임역원 그리드가 쓰던
  값 기반 충돌 감지가 이미 들어 있습니다. `lib/admin/registrations.ts` 의
  `updateRegField(id, expected, patch)` 가 필드별 검증만 얹어 감쌉니다.
  **통째 저장을 쓰면 안 되는 이유**: 내가 안 건드린 칸까지 내가 열었을 때의 값으로
  되돌아가고, 그 사이 다른 사람이 고친 것이 조용히 덮입니다.
- 편집 대상은 **id 로만** 들고 있습니다(`{mode:"edit", id}`). 행 스냅샷을 들고 있으면
  저장 후 새로고침된 값이 서랍에 안 비쳐 방금 고친 칸이 옛 값으로 보입니다.
- 다른 사람으로 갈아타면 `key={row.id}` 로 **서랍을 새로 마운트**해 입력 상태를 비웁니다.
  effect 로 되돌리면(react-hooks/set-state-in-effect 로도 막힙니다) 저장 직후
  새로고침에서 방금 친 글자가 서버 값으로 덮입니다.
- 좁은 화면(lg 미만)에서는 나란히 두지 않고 **오른쪽에서 덮는 패널**입니다.
  좁은 화면에서 split 하면 둘 다 못 읽습니다.
- 이동수단은 서랍 안에서 바꾸며, 타지구를 **확정으로** 바꾸면 §12 의 자동 반납이
  돌기 때문에 저장 전에 확인을 받습니다.

### 13-C. 함께 정리한 것 — 죽은 경로 제거

서랍이 수정을 가져가면서 `RegForm` 의 수정 경로와 `updateRegistrationFields` 가
호출처 없는 코드가 됐습니다. **남기지 않고 지웠습니다** — 남기면 "어느 화면에서
고쳤나"에 따라 동시 편집 결과가 달라지고, 이 레포는 죽은 모델을 남겨서
이미 한 번 물렸습니다(§0 의 5번, 죽은 preset 모델).

`RegForm` 은 이제 **추가 전용**입니다.

### 13-D. 검증

- **새 테스트 5개** (`tests/unit/reg-drawer.test.tsx`) — 저장 계약을 못 박습니다:
  고친 칸 하나만 보내는가 / `expected` 에 내가 보던 값이 실리는가 / "이용 안 함"이
  빈 문자열이 아니라 `null` 인가 / 안 바꾸면 안 보내는가 / 빈 비고가 `null` 인가.
  **이 화면은 master 로그인 뒤에만 열려 브라우저로 확인할 수 없고, 저장 버튼이 없어서
  잘못 보내도 아무도 눈치채지 못합니다.** 그래서 계약을 테스트로 세웠습니다.
- 게이트: tsc 0 · eslint 0 · **테스트 149** · build.

### 13-E. 🟡 사람 눈으로 확인해야 하는 것

로그인이 필요해 CC 가 자동 검증할 수 없습니다.

1. 연필 → 서랍이 오른쪽에 열리고 **표가 안 밀리는지**
2. 칸을 고치면 상단에 "○○ 저장됨" 이 뜨는지
3. 다른 사람 연필을 눌렀을 때 앞사람 입력이 안 남는지
4. 휴대폰에서 서랍이 오른쪽을 덮는지

---

## 14. B 완료 — 송금 "맞나요?" 승인 (2026-07-28)

§11-C 의 작업 순서 3번. **로컬 검증까지 끝났고 아직 배포 전입니다.**

### 14-A. 왜 이게 필요했는지 — 실측 숫자

운영 백업 기준입니다.

| | 값 |
|---|---|
| 총단이 "받았다"고 기록한 캠퍼스 | **17곳 중 14곳** |
| 총단 기록 합계 | **20,950,000원** |
| 캠퍼스가 등록한 송금 합계 | **0원** |

숫자는 **이미 총단 장부에 있었습니다.** 임역원에게 부족했던 건 정보가 아니라
"직접 계산해서 넣는 손"이었습니다. 그래서 계산을 없애고 **확인만** 하게 바꿉니다.

### 14-B. 어떻게 동작하나

`/campus/payments` 에 카드가 하나 뜹니다.

> 총단은 전남대에서 **2,000,000원**을 받았다고 기록했어요.
> 이 중 500,000원은 이미 등록돼 있어서, **1,500,000원**이 아직 등록되지 않았습니다. 맞나요?
> `[맞아요 — 1,500,000원 등록]` `[금액이 달라요]`

- 등록 금액은 총단 기록 **전체가 아니라 차이**입니다. 전체를 넣으면 이미 등록한 몫이
  이중 계상됩니다.
- **RPC 를 새로 만들지 않았습니다.** 기존 `campus_remit_add(금액, 메모)` 에 메모를
  `'총단 기록 확인'` 으로 넣습니다 — 누가 어떻게 등록했는지가 나중에 근거가 됩니다.
- **총단 장부는 바뀌지 않습니다.** 캠퍼스 쪽 기록만 총단 기록에 맞춥니다. 카드에도
  그렇게 적어 뒀습니다.
- 총단이 아직 안 적었으면(=`master_received_total` 0) 카드는 안 뜨고 **기존 유도 배너**가
  그대로 나옵니다. 승인식은 총단이 먼저 기록해야만 성립하므로 두 경로를 다 남겼습니다.
- 카드와 배너는 **동시에 뜨지 않습니다.** 둘 다 뜨면 "얼마를 등록하라는 건지"가
  두 가지가 돼 오히려 손이 멈춥니다.
- 배포된 **총단 1클릭 대리등록**(`masterRemitFor`)은 그대로 둡니다 — 같은 문제의 반대편입니다.

### 14-C. 검증

- DB 실측(롤백): 임역원 권한으로 **총단 기록을 읽을 수 있는가**(카드의 전제) →
  `campus_remit_add` 로 캠퍼스 누계 0 → 300,000 → 메모 `총단 기록 확인` 남는가. 전건 통과.
- **새 테스트 5개** (`tests/unit/campus-payments.test.tsx`): 차이만 등록하는가 ·
  카드와 배너가 배타인가 · 총단 기록 없으면 배너인가 · 이미 맞춰졌으면 안 뜨는가 ·
  "금액이 달라요" 가 직접 입력 경로를 남기는가.
- 게이트: tsc 0 · eslint 0 · **테스트 154** · build.

### 14-D. 남은 것 — §11-C 의 D

D(수송 요청 + 참여기간). **새 필드가 필요한 유일한 항목**이라 마지막입니다. 2~3일.

---

## 15. 배포 완료 (2026-07-28) — C+E · A · B

**세 덩어리가 운영에 올라갔습니다.** 동규님이 "먼저 배포"를 선택하셔서, D 를 만들기
전에 운영에서 확인할 수 있게 했습니다.

### 15-A. 무엇을 어떤 순서로

1. **백업** — `~/Backups/carbus-web/2026-07-28_1104-pre-transport`
   (15개 테이블 · 28MB · 실패 0). 파일 크기까지 확인한 뒤 진행했습니다.
2. **DB** — `supabase db push` 로 `20260728070000_transport_confirm_release.sql` 적용.
   **운영에서도 자체검증 10건 전건 통과** (검증 ⑤ 는 운영 실데이터로 환불 25,000원을 실측).
3. **확인** — 새 뷰 `v_transport_legs_detail` 조회 OK · 신청 599행 그대로.
4. **코드** — `transport-confirm` → `master` 머지(`a05c1d9`) 후 push. Vercel 자동 배포.

### 15-B. 🟡 운영에서 눈으로 확인할 것 (로그인 필요 — CC 가 못 함)

1. `/admin/e/<행사>/transport` — nav "이동수단". 지금은 `transport_legs` 가 0행이라
   "확정을 기다리는 타지구 차량이 없습니다 ✓" 만 보이는 게 정상입니다.
2. `/admin/e/<행사>/registrations` — 연필 → **오른쪽 서랍**. 표가 안 밀리는지,
   칸을 고치면 "○○ 저장됨" 이 뜨는지, 다른 사람으로 옮겨도 앞사람 입력이 안 남는지.
3. `/campus/payments` (임역원 계정) — **승인 카드가 14개 캠퍼스에 뜹니다.**
   총단 기록 합계 20,950,000원 대비 캠퍼스 등록 0원이라, 배포 직후 가장 눈에 띄는 변화입니다.
4. 휴대폰에서 서랍이 오른쪽을 덮는지.

### 15-C. 되돌리려면

DB 는 **백업 복원뿐**입니다(§3). 다만 이번 마이그레이션은 컬럼을 바꾸지 않아
(트리거 1 · 뷰 1 · 함수 2) 코드만 되돌려도 화면은 이전 상태로 돌아갑니다.
트리거를 끄고 싶으면:

```sql
alter table public.transport_legs disable trigger trg_transport_legs_zz_release;
```

이러면 자동 반납만 멈추고 나머지는 그대로입니다. 다시 켤 때는 `enable always` 로 켜세요
(`enable trigger` 만 쓰면 ORIGIN 으로 내려가 백업 적재에서 조용히 빠집니다).

---

## 16. D 완료 — 수송 요청 + 참여기간 (2026-07-28)

§11-C 의 **마지막 항목**입니다. 목업 결정 4가지가 전부 구현됐습니다.

### 16-A. 무엇이 달라지나

부분참자가 "며칠부터 며칠까지 있고, 언제 어디로 데리러 와 달라"고 말하는 두 가지가
지금까지는 **전부 비고에 글로** 적혔습니다. 그래서 누가 언제까지 있는지 세려면 사람이
비고를 읽어야 했고, "화요일 밤에 역으로 몇 명 나가야 하나"는 아무도 한눈에 못 봤습니다.

- **참여기간** — `registrations.attend_from` / `attend_to` (date). 비우면 행사 전체 참석.
- **수송 요청** — `pickup_requests`. 부분참 화면 하단의 **보드**가
  `(날짜, 시각, 장소)` 로 묶어 보여줍니다. **그게 그대로 간사 차량 배차표입니다.**

### 16-B. 설계에서 지킨 것

1. ⚠️ **픽업 장소에 지명을 박지 않았습니다** (동규님 지시 — "이번엔 평창역이지만
   앞으로 달라진다"). `place` 는 자유 입력 텍스트고, **장소 마스터 테이블을 만들지
   않았습니다.** 표기 통일은 같은 행사에서 이미 쓰인 값을 자동완성(datalist)으로
   보여주는 것으로 합니다.
2. **시각·장소를 몰라도 등록됩니다.** "가긴 가는데 아직 모른다"가 실제로 가장 흔한
   상태입니다. 필수로 받으면 아무 값이나 찍히고 **"미정"이 데이터에서 사라져 할 일이
   안 보이게** 됩니다. `pickup_at` NULL 은 보드 맨 위 **빨강 "시각 미정" 묶음**으로
   모입니다 — 그게 곧 다음에 물어봐야 할 사람들의 명단입니다.
3. **한 사람 한 방향 유니크를 걸지 않았습니다.** 부분참은 중간에 합류하고 중간에
   빠지는 일이 실제로 있어서(참여기간 컬럼을 두는 이유가 그것), 유니크를 걸면 표현
   자체가 막힙니다. 나중에 조일 수는 있어도 운영 중에 "적을 수가 없는" 상황은
   되돌릴 방법이 없습니다.
4. **참여기간에 행사 기간 체크를 걸지 않았습니다.** 행사 날짜가 나중에 바뀌면 이미
   들어간 값이 통째로 제약 위반이 돼 저장이 전부 막힙니다. 거꾸로 된 기간
   (`attend_from > attend_to`)만 막고 나머지는 화면 경고로 둡니다.
5. **KST 를 코드에서 못 박습니다.** `datetime-local` 값에는 타임존이 없어서, 그대로
   보내면 서버 시간대로 해석됩니다. 밤 늦은 픽업(막차·야간 도착)이 흔한 서비스라
   **하루가 밀리면 곧 다른 날 배차표**가 됩니다. 뷰도 `at time zone 'Asia/Seoul'` 로 끊습니다.

### 16-C. 어디서 넣고 어디서 보나

- **넣는 곳** = 편집 서랍(`/admin/e/<행사>/registrations` → 연필). 참여기간 두 칸과
  수송 요청 추가·삭제가 들어갔습니다. 사람 단위 편집은 이미 서랍이 맡고 있어서
  새 화면을 만들지 않았습니다.
- **보는 곳** = 부분참 화면(`/admin/e/<행사>/partial`) 하단 보드 + 표의 참여기간 열.
  묶음 단위로 읽는 자리입니다.

### 16-D. 새 테이블이라 함께 한 것 (§11-F 6)

`backup-prod.mjs` · `load-backup.py` · `post-load.sh`(ENABLE ALWAYS 복구 목록 + 검사)
**세 곳 모두** 등록했습니다. 행사 범위 3종 세트(`event_scope` 정책 ·
`guard_event_writable` ENABLE ALWAYS · 교차 행사 참조 가드)도 붙였습니다.

### 16-E. 검증

- 마이그레이션 자체검증 7건: 시각·장소 미정 등록 · 공백 장소 → NULL(묶음이 쪼개지지
  않게) · **KST 날짜 분해**(23:30 이 다음 날로 안 밀리는지) · 같은 사람 복수 등록 ·
  거꾸로 된 참여기간 거부 · 보드 노출.
- **교차 행사 가드는 두 번째 행사를 실제로 만들어 확인**했습니다(자체검증에서는 행사가
  하나뿐이라 건너뛰는 경로). 덤으로 `create_event` 가 지난 행사를 읽기 전용으로
  내리자 새 테이블 쓰기도 막히는 것을 확인 — 3종 세트가 제대로 붙었다는 증거입니다.
- **옛 백업 재적재**: `pickup_requests.json` 이 없는 백업으로도 적재가 깨지지 않습니다
  (로더가 "이 백업보다 나중에 생긴 테이블"로 안내하고 넘어감). 빈 DB 전량 재적용 →
  재적재 → **기준선 완전 일치**.
- 실데이터 예행(롤백): 시각 미정 3명이 맨 위, (8/11 23:30 · 어딘가 역) 2명,
  (8/12 09:00) 1명으로 묶임.
- **새 테스트 7개**(`tests/unit/pickup-board.test.tsx`): KST 오프셋 · 같은 묶음 합치기 ·
  장소 다르면 분리 · 미정이 맨 위 + 건수 경고. **틀리면 다른 날 배차표가 되는데
  화면으로는 그럴싸해 보여서 눈으로 못 잡습니다.**
- 게이트: tsc 0 · eslint 0 · **테스트 161** · build.

### 16-F. 🟡 사람 눈으로 확인할 것

1. 서랍에서 수송 요청 추가 → 부분참 보드에 묶여 뜨는지
2. 장소를 두 번째로 입력할 때 **자동완성 후보**로 뜨는지
3. 밤 11시 30분 픽업이 **그 날짜로** 보이는지 (하루 안 밀리는지)
